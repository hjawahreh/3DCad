# Rust Crates

Rust crates implement trusted native adapters and composition roots. The planned crate map is defined in `ARCHITECTURE.md`. A crate joins the Cargo workspace only when it has an approved public boundary, ownership metadata, and tests.
