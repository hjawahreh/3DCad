# Human Review (CLN-009)

Mandatory review before acceptance.

Supported actions: select, inspect confidence, relabel FDI, semantic relabel, merge, split, mark unknown, reject, re-run, undo/redo (after accept).

Visualization-only actions do not create history. Accepted segmentation and post-accept document restores do.
