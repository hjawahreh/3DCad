# Segmentation Benchmarks

```bash
pnpm --filter @cad-studio/studio test -- test/clinical/segmentation/segmentation.test.ts
```

Smoke harness: `runSegmentationBenchmarkSmoke()` — small/medium/large synthetic meshes. Timings are not CI gates.
