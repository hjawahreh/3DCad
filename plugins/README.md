# Plugins

Plugins are separately packaged, capability-scoped extensions. First-party plugins live under `plugins/first-party` only when they have a published SDK contract and complete package documentation. Plugins cannot depend on private core packages.
