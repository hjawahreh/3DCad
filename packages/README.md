# TypeScript Packages

Packages are independently owned bounded modules. The planned package map and allowed dependency direction are authoritative in `ARCHITECTURE.md`. No package directory is created until it can meet `docs/engineering/PACKAGE_STANDARD.md`; this prevents empty placeholder modules from becoming accidental public APIs.
