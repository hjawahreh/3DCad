# Testing

Unit/architecture tests cover family registry, Kernel Session caches, stub execute success/failure, unsupported ops, and absence of document APIs.

`pnpm --filter @cad-studio/geometry-services test`
