# API

- `GeometryServices` — register/replace families, open/close session, `execute`
- `KernelSession` — `cacheResource`, topology cache, `execute`
- `KernelBridgePort` / `MockKernelBridge`
- `createStubService(family)`
- `GEOMETRY_SERVICE_FAMILIES`

Semver `0.x`. **Stability: Experimental** (pre-PC-001). Bridge contract changes require an ADR.
