{
  "name": "@cad-studio/project-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "lint": "eslint src test",
    "typecheck": "tsc --build tsconfig.json --pretty false",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "bench": "vitest bench"
  },
  "dependencies": {
    "@cad-studio/platform-runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
