# Tooling

Repository tooling enforces policy rather than product behavior. Scripts and rules here must be deterministic, documented, versioned, and safe to run in CI. Dependency rules are defined in `dependency-rules.cjs`.
