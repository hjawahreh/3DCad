name: Nightly

on:
  schedule:
    - cron: '17 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  benchmark-and-audit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Record nightly status
        run: echo 'Benchmark corpus and artifact pipeline are introduced with their owning subsystems.'
